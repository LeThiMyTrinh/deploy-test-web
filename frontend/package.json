{
  "name": "deploy-test-frontend",
  "version": "1.0.0",
  "description": "Minimal static Frontend (HTML/CSS/JS thuần) — hiển thị Todo list lấy từ Backend API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'No build step required — static HTML/CSS/JS'"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
